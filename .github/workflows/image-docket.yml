# name: Build and Push to GAR

# on:
#   push:
#     branches:
#       - main

# env:
#   PROJECT_ID: fluted-polymer-376517 # TODO: update Google Cloud project id
#   GAR_LOCATION: us-central1-docker.pkg.dev # TODO: update Artifact Registry location
#   REPO_NAME: ez-repo

# jobs:
#   deploy:
#     permissions:
#       contents: 'read'
#       id-token: 'write'

#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v2

#       # Authenticate Docker to Google Cloud Artifact Registry
#       - name: Docker Auth
#         id: docker-auth
#         uses: 'docker/login-action@v1'
#         with:
#           username: _json_key
#           password: '${{ secrets.GCP_CREDENTIALS }}'
#           registry: '${{ env.GAR_LOCATION }}'

#       - name: Build and Push Container
#         run: |-
#           docker build -t "${{ env.GAR_LOCATION }}/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/frontend:${{ github.sha }}" ./
#           docker push "${{ env.GAR_LOCATION }}/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/frontend:${{ github.sha }}"
