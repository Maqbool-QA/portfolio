# name: Build and Push to GAR

# on:
#   push:
#     branches:
#       - prod

# env:
#   PROJECT_ID: fluted-polymer-376517 # TODO: update Google Cloud project id
#   GAR_LOCATION: us-central1-docker.pkg.dev # TODO: update Artifact Registry location
#   REPO_NAME: test
#   APP_REPO: argocd
#   QA_REPO: Automation

# jobs:
#   deploy:
#     permissions:
#       contents: 'read'
#       id-token: 'write'
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#         with:
#           repository: ${{ github.repository_owner }}/${{ env.QA_REPO }}
#           token: ${{ secrets.AUTOMATION_REPO_TOKEN }}
#       #           ref: main
#       - name: Cypress run
#         uses: cypress-io/github-action@v5
#         continue-on-error: true
#         with:
#           start: |
#             npm run dev_run
#           #           working-directory: /home/runner/work/frontend/
#           config-file: config/easy_ops_dev.js
#           spec: 'eazyops/Specs/**.spec.js'
#       - name: Checkout
#         uses: actions/checkout@v2

#       # Authenticate Docker to Google Cloud Artifact Registry
#       - name: Docker Auth
#         id: docker-auth
#         uses: 'docker/login-action@v1'
#         with:
#           username: _json_key
#           password: '${{ secrets.GCP_CREDENTIALS }}'
#           registry: '${{ env.GAR_LOCATION }}'

#       - name: Build and Push Container
#         run: |-
#           docker build -t "${{ env.GAR_LOCATION }}/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/frontend-prod:${{ github.sha }}" ./
#           docker push "${{ env.GAR_LOCATION }}/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/frontend-prod:${{ github.sha }}"
#       - uses: actions/checkout@v3
#         with:
#           repository: ${{ github.repository_owner }}/${{ env.APP_REPO }}
#           token: ${{ secrets.ARGO_REPO_TOKEN }}
#           ref: yamls

#       - uses: yokawasa/action-setup-kube-tools@v0.9.2
#         with:
#           setup-tools: |-
#             kustomize
#           kustomize: '5.0.1'
#       - run: |-
#           kustomize version
#           cd apps/prod/frontend
#           kustomize edit set image "${{ env.GAR_LOCATION }}/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/frontend-prod:${{ github.sha }}"
#           kustomize build
#           cat kustomization.yaml
#           # git add .
#           # git commit -m 'BASE image update'

#       - uses: stefanzweifel/git-auto-commit-action@v4
#         with:
#           branch: yamls
#           commit_message: frontend image tag pushed '${{  github.sha  }}'
#   slack-workflow-status:
#     if: always()
#     name: Post Workflow Status To Slack
#     needs:
#       - deploy
#     runs-on: ubuntu-latest
#     # actions.read permission is required.
#     permissions:
#       actions: 'read'
#     steps:
#       - name: Slack Workflow Notification
#         uses: Gamesight/slack-workflow-status@master
#         with:
#           # Required Input
#           repo_token: ${{secrets.GITHUB_TOKEN}}
#           slack_webhook_url: ${{secrets.SLACK_WEBHOOK_URL}}
#           # Optional Input
#           channel: '#devops-ci'
#           name: 'EazyOps CI'
#           icon_emoji: ':poop:'
#           icon_url: 'https://avatars0.githubusercontent.com/u/1701160?s=96&v=4'
